// Create the student object
const student = {
  name: "John"
};

// Function to get all keys
function getKeys(obj) {
  return Object.keys(obj);
}

// Print result (needed for this platform)
console.log(getKeys(student));